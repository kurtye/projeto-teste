import type { PlayerAggregates } from './types';
import { PlaceHolderImages } from './placeholder-images';

// This file is now deprecated as we are fetching live data from Firestore.
// It is kept for reference but is no longer used by the application.

export const players: any[] = [];
